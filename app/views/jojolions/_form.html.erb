<%= form_for(@jojolion) do |f| %>
  <% if @jojolion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@jojolion.errors.count, "error") %> prohibited this jojolion from being saved:</h2>

      <ul>
      <% @jojolion.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
